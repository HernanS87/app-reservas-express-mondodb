import dotenv from "dotenv";
dotenv.config();
// Definimos esta configuración acá porque de esa manera lo podemos importar en app.js y cargar las variables de entorno antes de que se carguen los demás módulos que intenten acceder a las mismas
