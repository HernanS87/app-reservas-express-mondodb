{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Process",
      "port": 9230,  // Debe coincidir con el puerto que has elegido
      "restart": true,
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
